[phases.setup]
nixPkgs = ["python311", "gcc", "gfortran", "openblas", "lapack", "pkg-config"]

[phases.install]
cmds = [
  "pip install --no-cache-dir -r requirements.txt"
]

[phases.build]
cmds = [
  "echo 'Build completed successfully'"
]

[start]
cmd = "python railway_optimized.py" 